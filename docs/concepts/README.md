One markdown file per concept: the problem, why the naive approach
fails, the algorithm in prose and pseudocode, complexity, pitfalls,
references. No runnable implementations -- if you could paste it and
run it, it is too concrete.

SPEC.md references the files it expects.
